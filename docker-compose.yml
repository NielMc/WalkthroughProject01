version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      environment:
          - PORT=${PORT}
      ports:
          - ${PORT}:${PORT}
      volumes:
          - ./app_pages:/app_pages